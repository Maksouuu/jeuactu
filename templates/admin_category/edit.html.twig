{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <div class="mb-3">
    <h1>Editer la catégorie</h1>
    </div>

    <div class="mt-3">
    {{ include('admin_category/_form.html.twig',{'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mb-3 mt-3">
    <a class ='btn btn-primary' href="{{ path('app_admin_category_index') }}">Revenir a la liste</a>
    </div>
    {{ include('admin_category/_delete_form.html.twig') }}
{% endblock %}
