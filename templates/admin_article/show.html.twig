{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.content }}</td>
            </tr>
        </tbody>
    </table>

    <a class ="btn btn-primary" href="{{ path('app_admin_article_index') }}">Revenir à la liste</a>

    <a class ="btn btn-primary" href="{{ path('app_admin_article_edit', {'id': article.id}) }}">Modifier</a>

    {{ include('admin_article/_delete_form.html.twig') }}
{% endblock %}
